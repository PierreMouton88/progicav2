<header>
	{# <nav id="conNav" class="navbar justify-content-end">
													<ul class="navbar-nav me-5"></ul>
												</nav> #}
	<nav class="navbar navbar-expand-lg bg-body-tertiary">
		<div class="container-fluid">
			<a class="navbar-brand bg-light ms-5" href="#"><img class="" src="../images/our_images/ProgicaLogo_no-bg.png" alt=""></a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<ul class="navbar-nav me-auto mb-2 mb-lg-0">
					<li class="nav-item">
						<a class="text-light nav-link" aria-current="page" href="{{path ('app_main')}}">Accueil</a>
					</li>
					<li class="nav-item">
						<a class="text-light nav-link" href="{{path ('app_gite_index') }}">Nos gîtes</a>
					</li>
					<li class="nav-item">
						<a class="text-light nav-link" href="#">Tourisme</a>
					</li>
					<li class="nav-item">
						<a class="text-light nav-link" href="#">Contact</a>
					</li>
				</ul>
				<div class="me-5 d-flex">
					<ul class="navbar-nav me-4 mb-2 mb-lg-0">
						<li class="nav-item">
							<a href="{{ path('app_gite_new') }}" class="nav-link btn btn-outline-light">Renseigner votre bien</a>
						</li>
						<li class="nav-item">
							{% if app.user %}
								<a class="text-light nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
							{% else %}
								<a class="text-light nav-link" href="{{ path('app_login') }}">Connexion</a>
							{% endif %}
						</li>
						{% if app.user %}
							<li class="nav-item">
								<a class="text-light nav-link" href="{{ path('app_user_show') }}">Mon compte</a>
							</li>
						{% endif %}
					</ul>
					<form class="d-flex" role="search">
						<input class="rounded-0 form-control me-2 text-center" type="search" placeholder="Recherche par lieu" aria-label="Search">
						<button class="btn btn-outline-light" type="submit">Recherche</button>
					</form>
				</div>
			</div>
		</div>
	</nav>
</header>
