{% extends 'base.html.twig' %}

{% block title %}Eqp{% endblock %}

{% block body %}
    <h1>Eqp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eqp.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ eqp.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ eqp.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_eqp_index') }}">back to list</a>

    <a href="{{ path('app_eqp_edit', {'id': eqp.id}) }}">edit</a>

    {{ include('eqp/_delete_form.html.twig') }}
{% endblock %}
