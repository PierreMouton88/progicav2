<form method="post" action="{{ path('app_eqp_delete', {'id': eqp.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eqp.id) }}">
    <button class="btn">Delete</button>
</form>
