{% extends 'base.html.twig' %}

{% block title %}GiteService{% endblock %}

{% block body %}
    <h1>GiteService</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gite_service.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ gite_service.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gite_service_index') }}">back to list</a>

    <a href="{{ path('app_gite_service_edit', {'id': gite_service.id}) }}">edit</a>

    {{ include('gite_service/_delete_form.html.twig') }}
{% endblock %}
