<form method="post" action="{{ path('app_gite_service_delete', {'id': gite_service.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gite_service.id) }}">
    <button class="btn">Delete</button>
</form>
