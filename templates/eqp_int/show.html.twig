{% extends 'base.html.twig' %}

{% block title %}EqpInt{% endblock %}

{% block body %}
    <h1>EqpInt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eqp_int.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ eqp_int.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_eqp_int_index') }}">back to list</a>

    <a href="{{ path('app_eqp_int_edit', {'id': eqp_int.id}) }}">edit</a>

    {{ include('eqp_int/_delete_form.html.twig') }}
{% endblock %}
