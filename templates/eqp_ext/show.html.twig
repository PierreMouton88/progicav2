{% extends 'base.html.twig' %}

{% block title %}EqpExt{% endblock %}

{% block body %}
    <h1>EqpExt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eqp_ext.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ eqp_ext.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_eqp_ext_index') }}">back to list</a>

    <a href="{{ path('app_eqp_ext_edit', {'id': eqp_ext.id}) }}">edit</a>

    {{ include('eqp_ext/_delete_form.html.twig') }}
{% endblock %}
