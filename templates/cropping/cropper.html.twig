{% extends 'base.html.twig' %}

{% block body %}
	{{ form_start(form) }}
	{{ form_widget(form) }}

	<button class="btn btn-outline-info" type="submit">Crop it!</button>
	{{ form_end(form) }}
{% endblock %}
