<form method="post" action="{{ path('app_gite_eqp_ext_delete', {'id': gite_eqp_ext.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gite_eqp_ext.id) }}">
    <button class="btn">Delete</button>
</form>
