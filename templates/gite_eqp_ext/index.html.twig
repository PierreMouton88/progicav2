{% extends 'base.html.twig' %}

{% block title %}GiteEqpExt index{% endblock %}

{% block body %}
    <h1>GiteEqpExt index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for gite_eqp_ext in gite_eqp_exts %}
            <tr>
                <td>{{ gite_eqp_ext.id }}</td>
                <td>
                    <a href="{{ path('app_gite_eqp_ext_show', {'id': gite_eqp_ext.id}) }}">show</a>
                    <a href="{{ path('app_gite_eqp_ext_edit', {'id': gite_eqp_ext.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_gite_eqp_ext_new') }}">Create new</a>
{% endblock %}
